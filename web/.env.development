VITE_API_BASE_URL=http://localhost:5109/api
# When true, the frontend serves an in-browser mock backend instead of calling the .NET API.
# Flip to false once api/TeaShop.Api is running to develop against the real HTTP contract.
VITE_USE_MOCK=true
